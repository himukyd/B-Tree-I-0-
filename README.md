# B-Tree-I-0-
Disk-based B+ Tree index implemented in C++17. Supports persistent storage, insertion, deletion, point lookup, and range queries using fixed-size pages and POSIX file I/O. Includes a driver program, Makefile, and documentation for building and running on Linux/macOS.
